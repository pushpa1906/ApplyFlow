services:
  backend:
    build: ./backend
    env_file: ./backend/.env
    ports: ["8000:8000"]
    volumes:
      - ./backend/service-account.json:/app/service-account.json:ro
  frontend:
    build: ./frontend
    ports: ["5173:5173"]
    depends_on: [backend]
