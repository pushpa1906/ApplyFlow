import type { AppSettings } from "../types/settings";

export const DEFAULT_SETTINGS: AppSettings = {
  dailyGoal: 10,
  weeklyGoal: 70,
  celebrations: true,
};

export const DEFAULT_SORT_COLUMN = "Applied Date";
