import Button from "../components/common/Button";
import Card from "../components/common/Card";
import type { WorkspaceMode } from "../types/workspace";
interface Props{mode:WorkspaceMode;sheetId:string;serviceEmail:string;dailyGoal:number;weeklyGoal:number;celebrations:boolean;setDailyGoal:(value:number)=>void;setWeeklyGoal:(value:number)=>void;setCelebrations:(value:boolean)=>void;onSync:()=>void;onDisconnect:()=>void;}
export default function Settings({mode,sheetId,serviceEmail,dailyGoal,weeklyGoal,celebrations,setDailyGoal,setWeeklyGoal,setCelebrations,onSync,onDisconnect}:Props){return <div className="settings-grid"><Card><span className="eyebrow">Connection</span><h3 className="info-title">Workspace</h3><dl className="settings-list"><div><dt>Mode</dt><dd>{mode}</dd></div><div><dt>Spreadsheet ID</dt><dd className="mono">{sheetId||"Demo data"}</dd></div><div><dt>Service account</dt><dd>{serviceEmail||"Not configured"}</dd></div></dl><div className="settings-actions"><Button onClick={onSync}>Sync now</Button><Button variant="danger" onClick={onDisconnect}>Disconnect</Button></div></Card><Card><span className="eyebrow">Preferences</span><h3 className="info-title">Goals</h3><div className="settings-form"><label>Daily target<input type="number" min={1} value={dailyGoal} onChange={(event)=>setDailyGoal(Number(event.target.value))}/></label><label>Weekly target<input type="number" min={1} value={weeklyGoal} onChange={(event)=>setWeeklyGoal(Number(event.target.value))}/></label><label className="toggle-row"><span><strong>Celebrations</strong><small>Show completion messages when goals are reached.</small></span><input type="checkbox" checked={celebrations} onChange={(event)=>setCelebrations(event.target.checked)}/></label></div></Card></div>}
