import { BarChart3, CheckCircle2, Database } from "lucide-react";
import Choice from "../components/welcome/Choice";
interface Props{loading:boolean;error:string;sheetId:string;setSheetId:(value:string)=>void;connect:()=>void;openDemo:()=>void;personalAvailable:boolean;accessCode:string;setAccessCode:(value:string)=>void;openPersonal:()=>void;serviceEmail:string;}
export default function Welcome({loading,error,sheetId,setSheetId,connect,openDemo,personalAvailable,accessCode,setAccessCode,openPersonal,serviceEmail}:Props){return <main className="welcome-page"><section className="welcome-shell"><div className="welcome-heading"><div className="welcome-logo">AF</div><span className="eyebrow">Career management workspace</span><h1>ApplyFlow</h1><p>Turn your Google Sheets job tracker into a calm, focused dashboard you’ll enjoy opening every day.</p></div><div className={`choice-grid ${personalAvailable?"three":"two"}`}><Choice icon={<BarChart3/>} title="Explore Demo" text="Recruiter-safe sample data with working charts, filters, goals, and CRUD." button="Open demo" action={openDemo}/><Choice icon={<Database/>} title="Connect Google Sheet" text="Paste a spreadsheet URL or ID. The connection is remembered in this browser." button="Connect workspace" action={connect} loading={loading}><input value={sheetId} onChange={(event)=>setSheetId(event.target.value)} placeholder="Google Sheets URL or ID"/></Choice>{personalAvailable&&<Choice icon={<CheckCircle2/>} title="Personal Workspace" text="Protected access to your preconfigured private spreadsheet." button="Open personal workspace" action={openPersonal} loading={loading}><input type="password" value={accessCode} onChange={(event)=>setAccessCode(event.target.value)} placeholder="Private access code"/></Choice>}</div>{serviceEmail&&<p className="service-note">For read/write access, share your <strong>Google spreadsheet</strong> with <strong>{serviceEmail}</strong> as Editor.</p>}{error&&<div className="error-banner">{error}</div>}</section></main>}
